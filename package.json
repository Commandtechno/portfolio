{
  "scripts": {
    "build": "parcel build ./index.html --dist-dir build",
    "prebuild": "cd js && tsc && cd .. && ts-node --transpile-only ./scripts/prebuild.ts",
    "postbuild": "ts-node --transpile-only ./scripts/postbuild.ts"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.4.1",
    "@parcel/transformer-webmanifest": "^2.4.1",
    "@types/node": "^17.0.25",
    "cheerio": "^1.0.0-rc.10",
    "parcel": "^2.4.1",
    "sharp": "^0.29.3",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
